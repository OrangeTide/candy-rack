// SPDX-License-Identifier: 0BSD

// Bundle entry for the AudioWorklet global scope. Importing the runtime runs
// its registerProcessor call. Add future processors here as the engine roster
// grows.
import './runtime.js';
